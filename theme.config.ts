export type ThemeId = 'default' | 'warm' | 'dark-pro' | 'minimal';

// Change this to switch the blog's design theme at build time.
// 'default' | 'warm' | 'dark-pro' | 'minimal'
export const ACTIVE_THEME: ThemeId = 'dark-pro';
